{
  "name": "chainlink-token-subgraph",
  "version": "0.2.0",
  "description": "Chainlink Token Subgraph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio a298d9e05ce0c2849707c1f7c1bd7013",
    "deploy": "graph deploy --studio demo",
    "create-local": "graph create --node http://localhost:8020/ chainlink-token-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ chainlink-token-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chainlink-token-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  }
}
